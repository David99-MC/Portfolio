function WorkRelevant({ children }: { children: React.ReactNode }) {
  return (
    <p className="font-mono text-lg font-semibold text-slate-500 uppercase tracking-[0.25em]">
      {children}
    </p>
  );
}

export default WorkRelevant;
